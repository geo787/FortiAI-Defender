# FortiAI-Defender User Guide

## Overview
FortiAI-Defender is a cybersecurity tool that monitors networks in real-time, detects anomalies with AI, and simulates responses like blocking malicious IPs.

## Installation
1. Ensure Python 3.8+ is installed.
2. Navigate to the `FortiAI-Defender/backend/` directory.
3. Install dependencies:
   ```bash
   pip install -r requirements.txt
